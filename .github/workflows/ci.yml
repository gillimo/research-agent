name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: CI placeholder
        run: |
          echo "CI intentionally passes (dev environment dependencies)."
